/**
 * Placeholder seeding helper retained for backwards compatibility.
 * The new CampaignContent model allows campaigns to be seeded via
 * standalone scripts like `scripts/quickSeed.ts`, so this function
 * intentionally does nothing.
 */
export async function ensureSeedData(): Promise<void> {
  return;
}
